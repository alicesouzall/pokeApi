import { styled } from "styled-components";

export const DynamicButtonStyle = styled.button `
    display: flex;
    align-items: center;
    justify-content: center;
    border: none;
    cursor: pointer;
    background-color: #fff;

    label {
        cursor: pointer;
    }
`;
